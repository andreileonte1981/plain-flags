{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "merge-env",
            "type": "shell",
            "command": "cat ../../infrastructure/.secrets/firebase.env .env > .env.merged",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}