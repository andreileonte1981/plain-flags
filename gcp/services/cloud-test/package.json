{
  "name": "plainflags-cloud-test",
  "version": "1.0.0",
  "description": "Cloud-deployable test service for Plain Flags management API",
  "main": "dist/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "compile": "rm -rf dist && tsc",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "local": "npm run compile && env-cmd -f .env.merged node dist/engine/run-local.js",
    "predeploy": "npm run compile"
  },
  "dependencies": {
    "axios": "^1.8.3",
    "dotenv": "^16.4.5",
    "env-cmd": "^11.0.0",
    "fastify": "^5.1.0",
    "pino-pretty": "^11.0.0",
    "plain-flags-node-sdk": "^1.1.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.3.0"
  },
  "author": "Andrei Leonte",
  "license": "ISC"
}
