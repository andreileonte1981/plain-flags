from setuptools import setup, find_packages

setup(
    name="plain-flags-python-sdk-tests",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    long_description="Plain Flags Python SDK tests",
    long_description_content_type="text/markdown"
)
